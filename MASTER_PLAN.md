# الماستر بلان — عالم مهووس وموسوعة العملاء

تاريخ الخطة: 4 أغسطس 2026  
النطاق: مساعد شراء العطور، محرك التعاطف، آراء العملاء الموثقة، وتكامل Salla.

## 1. الملخص التنفيذي

أصبح المشروع قابلاً للتشغيل محلياً عبر Flask وStreamlit، مع بيئة Python موحدة
واختبارات آلية شاملة. عولجت مشكلات الاعتمادات والترميز، حُميت مسارات الإدارة،
وتم توحيد تحميل `.env` من دون استبدال إعدادات الاستضافة. كما استُبدلت معاملات
Streamlit القديمة بواجهات الإصدار المثبت 1.60.

أضيف `empathy_engine.py` لبناء شخصيات بحثية عميقة تتضمن سياق حياة وذكرى شمية
مفترضة واحتياجات ثقة وشرط ولاء وصوتاً محلياً. كل مخرج اصطناعي يحمل حتمياً
`synthetic=true` و`publishable=false`. بالتوازي، يتيح `customer_feedback.py`
جمع تفاصيل حقيقية بأسئلة محايدة، ولا يبني سجل نشر إلا عند وجود مرجع طلب وموافقة.

الهدف الاستراتيجي ليس «ولاءً مطلقاً» مصطنعاً؛ بل ولاء قابل للقياس ينشأ من
ترشيح صادق، تجربة متسقة، استجابة جيدة للملاحظات، وذاكرة اختيار تحترم العميل.

## 2. مبادئ المنتج

1. الحقيقة قبل الإقناع: لا ادعاء ثبات أو تغليف أو تجربة إلا من كتالوج موثق أو
   رأي عميل ذي مصدر وموافقة.
2. افصل البحث عن النشر: الشخصيات الافتراضية تختبر الأفكار، ولا تدخل جدول
   المراجعات العامة مطلقاً.
3. ذاكرة نافعة وقابلة للنسيان: كل ذكرى لها غرض ومصدر ومستوى ثقة وتاريخ حذف.
4. أقل صلاحية وأقل بيانات: لا نسخ شامل من Salla؛ اجلب ما يلزم للحالة فقط.
5. الولاء نتيجة، لا نص برومبت: يقاس بالتكرار، جودة الترشيح، والمناصرة الطوعية.

## 3. خارطة الطريق — 12 أسبوعاً

### الشهر الأول: الأساس الموثوق (الأسابيع 1–4)

#### الأسبوع 1: قياس وبنية تشغيل

- نقل التطبيق إلى إعداد إنتاج خلف WSGI على Linux مع Health check وسجلات JSON.
- إضافة CI يشغّل `pytest`, `compileall`, وفحص الأسرار عند كل Push.
- إنشاء بيئات `dev/staging/prod` ومفاتيح مستقلة مع تدوير موثق.
- خط أساس KPIs: معدل التحويل، الشراء المتكرر 30/60/90 يوماً، المرتجعات،
  معدل إرسال رأي موثق، ورضا الترشيح.

#### الأسبوع 2: موصل Salla آمن

- OAuth بصلاحيات الحد الأدنى وتخزين الرموز مشفرة.
- مستقبل Webhooks يتحقق من توقيع/رمز Salla، يسجل `event_id` لمنع التكرار،
  ثم يضع الحدث في Queue مع Dead-letter للأخطاء.
- ابدأ بالأحداث: العميل، الطلب، تحديث حالة الطلب، و`review.added`.
- Backfill محدود للطلبات اللازمة فقط، ثم اعتماد Webhooks للتحديثات اللحظية.

#### الأسبوع 3: حلقة الرأي الحقيقي

- بعد اكتمال/تسليم الطلب، أرسل طلب رأي محايد بعد مدة قابلة للضبط.
- استخدم أسئلة `build_experience_questions` عن أول تجربة، تغير الرائحة، التغليف،
  الملاحظة السلبية، والتوصية؛ لا تسأل سؤالاً يقود للمدح.
- اربط الرأي بمرجع الطلب، موافقة النشر، نسخة سياسة الموافقة، وقناة الجمع.
- اسمح للعميل باختيار: خاص لتحسين الخدمة / عام باسم أول / عام مجهول.

#### الأسبوع 4: مساعد اختيار قائم على الأدلة

- Quiz من 6–8 أسئلة: المناسبة، العائلة العطرية، الحساسية للقوة، الجو، الميزانية،
  وروائح أحبها/لم يحبها العميل.
- محرك Ranking قابل للتفسير يعرض ثلاثة خيارات و«لماذا رشحناها» و«ما الذي قد لا
  يناسبك»، مع مستوى ثقة.
- لا يستخدم النموذج التوليدي إلا لصياغة الشرح من حقائق مسترجعة ومسموح بها.

**بوابة نهاية الشهر:** صفر Webhook غير موثق مقبول، ≥95% أحداث بلا تكرار،
و100% من المراجعات العامة ذات مصدر وموافقة.

### الشهر الثاني: بناء العالم والعلاقة (الأسابيع 5–8)

#### الأسبوع 5: «جوازك العطري»

- صفحة عميل تحفظ تفضيلات معلنة: العائلات المحبوبة، النفور، المناسبة، القوة،
  والميزانية؛ كل حقل قابل للتعديل والحذف.
- سجل «جرّبت / أحببت / لم يناسبني» مع سبب اختياري ومستوى ثقة، لا استنتاج صامت.
- بطاقة مشاركة اختيارية غير شخصية: «ذوقي خشبي دافئ للمساء».

#### الأسبوع 6: «دفتر الروائح»

- مساحة قصص حقيقية بموافقة: ذكرى مرتبطة برائحة، مناسبة، أو شخص عزيز، مع تحكم
  العميل في الاسم والظهور.
- مراجعة بشرية قبل النشر، وإزالة بيانات الأطراف الثالثة.
- تحويل القصص المصرح بها إلى مجموعات موضوعية: الجمعة، السفر، التخرج، الهدية.

#### الأسبوع 7: رفيق ما بعد الشراء

- تذكير اختياري لتجربة العطر في أجواء/أوقات مختلفة، ثم تسجيل الملاحظة.
- توصية بطبقات العطر أو عينة مكملة بناء على المشتريات والتفضيلات، مع سبب واضح.
- Playbook خدمة: إذا ظهر عدم رضا، افتح مسار مساعدة قبل أي طلب مراجعة عامة.

#### الأسبوع 8: عالم محتوى صادق

- سلاسل: «رائحة موقف»، «ماذا يعني الثبات فعلاً؟»، و«اختيار هدية بلا تخمين».
- استخدم الشخصيات الاصطناعية لاختبار وضوح الرسائل داخلياً، ثم اختبر النسخ
  النهائية A/B على جمهور حقيقي بموافقة وبلا اقتباسات مزيفة.
- أنشئ بنك UGC موثقاً بحقوق الاستخدام وتاريخ انتهاء الإذن.

**بوابة نهاية الشهر:** تحسن معدل إكمال Quiz، انخفاض المرتجعات بسبب عدم ملاءمة
الرائحة، وارتفاع نسبة الملاحظات النوعية الحقيقية لا مجرد تقييم النجوم.

### الشهر الثالث: التخصيص والولاء القابلان للتوسع (الأسابيع 9–12)

#### الأسبوع 9: نموذج ذوق هجين

- دمج قواعد العائلات/المكونات مع Collaborative signals مجهولة أو مجمعة.
- احتساب `recommendation_confidence` وسبب الاستبعاد، لا ترتيب غامض فقط.
- Offline evaluation: Precision@3، قبول الترشيح، والمرتجعات بعد الترشيح.

#### الأسبوع 10: شرائح لحظية مسؤولة

- شرائح سلوكية: جديد متردد، باحث هدية، متذوق نيش، عائد بعد تجربة ناجحة.
- رسائل مختلفة حسب حاجة القرار، لا حسب سمات حساسة أو استنتاجات شخصية دقيقة.
- Frequency caps وإيقاف تلقائي عند الانسحاب أو ظهور إزعاج.

#### الأسبوع 11: برنامج ولاء «الأثر»

- نقاط للسلوك النافع: إكمال ملف الذوق، رأي صادق، تحديث ملاحظة بعد أسبوع،
  وإحالة مؤكدة؛ لا مكافأة مشروطة بمراجعة إيجابية.
- مزايا: عينات مختارة، وصول مبكر، جلسة ترشيح، وهدية مناسبة للذوق.
- تفسير واضح لسبب كل ميزة ومنع التلاعب أو الحسابات الاصطناعية.

#### الأسبوع 12: الإطلاق والتحسين

- تجربة Holdout تقيس الأثر الحقيقي مقابل مجموعة لا تتلقى تخصيصاً.
- مراجعة الخصوصية، اختبار حذف/تصدير بيانات عميل، وتمرين استعادة من النسخ.
- لوحة تشغيل أسبوعية: الإيراد المتكرر، رضا الترشيح، صحة Webhooks، الموافقات،
  الانسحابات، الشكاوى، وعدالة أداء الترشيح بين الشرائح.

**بوابة نهاية الربع:** أثر موجب إحصائياً على الشراء المتكرر أو رضا الاختيار،
من دون ارتفاع الانسحاب، الشكاوى، أو المرتجعات.

## 4. معمارية Salla المقترحة

```text
Salla OAuth + Webhooks
        |
        v
Webhook gateway -- verify signature/token --> event inbox (idempotent)
        |                                      |
        | invalid -> reject + security log     v
        |                                  queue / retry / DLQ
        |                                      |
        +--------------------------------------v
                                customer memory service
                                  |        |        |
                                  v        v        v
                             recommender  feedback  analytics
                                  |        |        |
                                  +--------+--------+
                                           v
                                  Streamlit/Admin API
```

ضوابط التنفيذ:

- تحقق من التوقيع/الرمز قبل قراءة Payload، ثم تحقق من Schema والحجم والزمن.
- مفتاح Idempotency مركب من `merchant_id + event + event_id/created_at + hash`.
- لا تنفذ عملاً ثقيلاً داخل طلب Webhook؛ أعد 2xx بعد الحفظ الموثوق ثم عالج Queue.
- راقب Changelog في CI شهرياً، خاصة الحقول المنقولة إلى كائن `urls`.
- احفظ Payload الخام مشفراً لفترة قصيرة للتشخيص، ثم احتفظ بالحقول الضرورية فقط.

## 5. قاعدة بيانات «موسوعة العملاء»

الاختيار المقترح: PostgreSQL. افصل PII في Schema/Vault مستقل عن ذاكرة الذوق،
وافصل الشخصيات الاصطناعية في قاعدة أو Schema لا يملك مفتاحاً إلى العملاء.

### 5.1 الجداول الأساسية

| الجدول | الغرض | أهم الحقول |
|---|---|---|
| `customer_identity` | ربط داخلي بـSalla وPII مشفر | `customer_id`, `merchant_id`, `salla_customer_id`, `email_enc`, `phone_enc`, `status` |
| `customer_profile` | بيانات غير حساسة قابلة للتعديل | `customer_id`, `preferred_locale`, `city_code`, `profile_version`, `updated_at` |
| `consent_ledger` | سجل موافقات غير قابل للطمس | `consent_id`, `customer_id`, `purpose`, `policy_version`, `granted_at`, `withdrawn_at`, `channel`, `proof_hash` |
| `source_event_inbox` | استقبال Webhooks ومنع التكرار | `event_key`, `merchant_id`, `event_type`, `payload_enc`, `received_at`, `processed_at`, `status`, `retry_count` |
| `orders` | الحد الأدنى اللازم لتأكيد العلاقة | `order_id`, `customer_id`, `salla_order_id`, `status`, `ordered_at`, `completed_at` |
| `order_items` | المنتجات الفعلية | `order_item_id`, `order_id`, `product_id`, `variant_id`, `quantity` |
| `scent_preferences` | تفضيلات معلنة/مستنتجة بشفافية | `preference_id`, `customer_id`, `dimension`, `value`, `polarity`, `source_type`, `confidence`, `observed_at` |
| `memory_entries` | ذكريات شمية حقيقية أو خاصة | `memory_id`, `customer_id`, `memory_type`, `text_enc`, `visibility`, `source_reference`, `consent_id`, `verification_status`, `retention_until` |
| `feedback` | رأي أصلي ومصدره | `feedback_id`, `customer_id`, `order_id`, `product_id`, `original_text_enc`, `public_text`, `rating`, `publish_status`, `consent_id` |
| `interactions` | إشارات رحلة العميل | `interaction_id`, `customer_id`, `event_type`, `product_id`, `channel`, `occurred_at`, `metadata_json` |
| `recommendations` | أثر كل ترشيح | `recommendation_id`, `customer_id`, `model_version`, `candidates_json`, `reason_codes`, `confidence`, `accepted_product_id`, `outcome_at` |
| `loyalty_events` | دفتر نقاط قابل للتدقيق | `loyalty_event_id`, `customer_id`, `action`, `points_delta`, `reason`, `source_reference`, `created_at` |
| `data_subject_requests` | وصول/تصحيح/حذف/سحب موافقة | `request_id`, `customer_id`, `request_type`, `opened_at`, `due_at`, `completed_at`, `evidence_uri` |
| `audit_log` | أثر إداري وأمني | `audit_id`, `actor_id`, `action`, `entity_type`, `entity_id`, `occurred_at`, `ip_hash`, `details_json` |

### 5.2 فصل المحتوى الاصطناعي

`research.synthetic_personas` و`research.synthetic_outputs` يحتويان `synthetic=true`
و`publishable=false` بقيد قاعدة بيانات `CHECK`. يمنع Foreign Key إلى
`customer_identity`، ويمنع أي Job نشر من القراءة من Schema `research`.

### 5.3 قيود مهمة

- `feedback.publish_status='published'` يتطلب `consent_id` صالحاً و`order_id`
  أو مصدراً موثقاً آخر، مع عدم سحب الموافقة.
- `memory_entries.visibility='public'` يتطلب موافقة منفصلة عن موافقة التخصيص.
- كل حقل مستنتج يحمل `source_type`, `confidence`, و`model_version`.
- `retention_until` إلزامي للـPayload الخام والذكريات الحرة؛ Job يومي يحذف المنتهي.
- Embeddings، إن استُخدمت، تعامل كبيانات مشتقة شخصية: تشفير، Namespace لكل متجر،
  وحذف متزامن مع المصدر.

## 6. KPIs وحواجز القياس

### القيمة

- نسبة إكمال مساعد الاختيار.
- قبول Top-3 وترجمته إلى إضافة للسلة/شراء.
- انخفاض المرتجعات المرتبطة بعدم ملاءمة الرائحة.
- الشراء المتكرر 30/60/90 يوماً، وIncremental lift عبر Holdout.
- نسبة العملاء الذين يقدمون ملاحظة نوعية طوعاً.

### الثقة والحماية

- 100% مراجعات عامة ذات Provenance وموافقة.
- 0 مخرج اصطناعي في قناة النشر.
- معدل الانسحاب والشكاوى وإلغاء الاشتراك.
- زمن تنفيذ سحب الموافقة والحذف.
- Webhook signature failures، duplicates، DLQ depth، ووقت المعالجة P95.

## 7. أول Backlog تنفيذي بعد الخطة

1. `salla_connector/` مع OAuth، التحقق من Webhooks، Inbox، وIdempotency.
2. Migration أولى لجداول الهوية والموافقة والأحداث والطلبات والآراء.
3. صفحة «جوازك العطري» وAPI لتعديل/حذف التفضيلات.
4. Workflow رأي ما بعد التسليم يستهلك `customer_feedback.py`.
5. Recommendation service بقواعد قابلة للتفسير قبل أي ML معقد.
6. CI/CD وStaging واختبار عقد Contract Test أسبوعي مع Salla.

## 8. مصادر رسمية راجعتها للخطة

- [Salla Webhooks والأحداث والتحقق الأمني](https://docs.salla.dev/421119m0)
- [Salla: قائمة Webhooks النشطة وصلاحية webhooks.read](https://docs.salla.dev/5394135e0)
- [Salla Merchant API changelog](https://docs.salla.dev/doc-421127)
- [النظام السعودي لحماية البيانات الشخصية — SDAIA](https://dgp.sdaia.gov.sa/wps/portal/pdp/knowledgecenter/details/PDPL/)

هذه الخطة هندسية وتشغيلية، وليست بديلاً عن مراجعة قانونية متخصصة لسياسة
الخصوصية، أساس المعالجة، وفترات الاحتفاظ الفعلية قبل الإطلاق العام.
